interface:
  display_name: "AI Small TDD Change"
  short_description: "Explicit lightweight TDD workflow for small scoped changes"
  default_prompt: "Use $ai-small-tdd-change for a small scoped change with RED/GREEN verification evidence."

policy:
  allow_implicit_invocation: false
