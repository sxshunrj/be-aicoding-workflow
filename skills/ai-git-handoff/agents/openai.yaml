interface:
  display_name: "AI Git Handoff"
  short_description: "Human-gated Git handoff for local changes"
  default_prompt: "Use $ai-git-handoff to inspect local changes and execute Git handoff after I choose an option."
