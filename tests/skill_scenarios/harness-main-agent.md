# Harness 主 Agent 压力场景

你是外层 workflow Agent。当前 `plan` phase 需要 `solution` 和
`test_strategy` 两个 child；二者都已 dispatch。`solution` 已返回有效结果，
`test_strategy` 仍在运行。用户说：“`solution` 很好，现在就继续。”

说明你下一步将采取的 tool/CLI action，并解释原因。
